{% set name = "ai4s-jobq" %}
{% set version = "3.11.1" %}
{% set suffix = ".post2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name | replace('-', '_') }}-{{ version }}.tar.gz
  sha256: 755aa5039d8ee65334a56fe79cae2ac9bcec4ec16625a6045d30ae1c5defdb89

build:
  entry_points:
    - ai4s-jobq = ai4s.jobq.cli:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - pip
    - python {{ python_min }}
    - setuptools >=35.0.2
    - setuptools-scm >=8
    - wheel >=0.29.0
  run:
    - humanize
    - tenacity
    - aiohttp
    - anyio
    - appdirs
    - asyncclick <8.2.2
    - azure-identity
    - azure-monitor-opentelemetry >=1.8.7
    - azure-servicebus
    - azure-storage-blob
    - azure-storage-queue
    - cachetools
    - joblib
    - psutil
    - python >={{ python_min }}
    - pyyaml
    - rich
    - types-python-dateutil

test:
  imports:
    - ai4s
  commands:
    - pip check
    - ai4s-jobq --help
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://aka.ms/jobq
  dev_url: https://github.com/microsoft/ai4s-jobq
  doc_url: https://microsoft.github.io/ai4s-jobq/
  summary: AI for Science Job Queue - A distributed job queue system for large scale embarassingly parallel workloads
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - awvwgk
